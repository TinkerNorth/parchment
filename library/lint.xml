<?xml version="1.0" encoding="utf-8"?>
<lint>
    <!-- Queries Maven Central as it runs, so an upstream release fails a build that changed
         nothing. Informational still reports it; Dependabot owns upgrades. -->
    <issue id="NewerVersionAvailable" severity="informational" />
    <!-- Queries the network as it runs, so an Android Gradle plugin release fails a build that
         changed nothing, the same way NewerVersionAvailable used to. Dependabot owns upgrades. -->
    <issue id="AndroidGradlePluginVersion" severity="informational" />
</lint>
